# Core dependencies
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.9.0
matplotlib>=3.6.0
seaborn>=0.12.0
scikit-learn>=1.2.0
gym>=0.26.0

# PyTorch (CPU or GPU)
# For CPU only:
torch>=1.13.0
# For GPU (CUDA 11.7 or higher) uncomment the line below and comment the CPU line:
# torch>=1.13.0+cu117 --index-url https://download.pytorch.org/whl/cu117

# Optional but recommended
jupyter>=1.0.0
tqdm>=4.64.0
tabulate>=0.9.0
